services:
  - type: web
    name: foodweb
    env: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: foodweb-db
          property: connectionString

databases:
  - name: foodweb-db
    plan: free
